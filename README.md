# Surround_Test
For testing surround AI
## This repository contains the python file named Test_surround_init_lint.py for testing surround init cli functionality.
**Create a surround project and execute the python file inside the project for testing the naming conventions 
and no of files generated in the project. It also verifies the file names of the surround project.**
